/* http://keith-wood.name/maxlength.html
   German initialisation for the jQuery Max Length extension */
(function($) { // hide the namespace

$.maxlength.regionalOptions['de'] = {
	feedbackText: '{r} verbleibende Zeichen ({m} maximum)',
	overflowText: '{o} Zeichen Überschuss ({m} maximum)'
};
$.maxlength.setDefaults($.maxlength.regionalOptions['de']);

})(jQuery);
